# 🌌 Teleport VX — система Neon Aurora

**Teleport VX** — это экспериментальная система загрузки и визуализации в неоновом стиле **Neon Aurora**,  
разработанная **Victor Labs / Aurora Division**.  
Она объединяет оболочку, ядро и модульную инфраструктуру с поддержкой автоматического сохранения, самодиагностики и симуляции загрузки.

---

## ✨ Особенности версии 2.3 — *Neon Expansion*

**Neon Expansion** — это обновление, расширяющее ядро и интерфейс VX, добавляющее новые инструменты диагностики и визуальные улучшения.

### 🧩 Новое в 2.3
- 🧠 Новый модуль `doctor.sh` — диагностика ядра, сети, времени, диска и RNG.  
- 💡 Командный интерфейс `vx` (status | log | version | boot | config | doctor).  
- 💾 Автосохранение и фоновый режим (`sv`, `svloop`).  
- 🌈 Обновлённый неоновый стиль Aurora.  
- 📘 Добавлены файлы `README` и `CHANGELOG`.

---

## ⚙️ Основные модули

| Модуль | Назначение |
|--------|-------------|
| **Boot** | Визуальный запуск ядра и инициализация среды |
| **Core** | Базовые подсистемы VX (vfs, net, time, rng) |
| **Modules** | Расширения и диагностика (`doctor.sh`, `autosave.sh`) |
| **Bin/VX** | Универсальный CLI-интерфейс команд |
| **Style** | Эстетика интерфейса в стиле Neon Aurora |
| **Docs** | Документация и схемы |

---

## 🧠 Команды VX

```bash
vx version   # версия системы
vx status    # текущий статус
vx doctor    # диагностика
vx log 10    # последние коммиты
vx boot      # запуск boot/teleport.sh
