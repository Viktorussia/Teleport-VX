# 🌀 Teleport VX — система Neon Aurora  
**Teleport VX** — экспериментальная система загрузки и визуализации в стиле *Neon Aurora*, разработанная Виктором.  
Она объединяет оболочку, ядро и модульную инфраструктуру с поддержкой автоматического сохранения, самодиагностики и симуляции загрузки.  

---

### 🚀 Основные модули
- **Boot** — визуальный старт ядра и инициализация среды  
- **Core** — базовые подсистемы VX (vfs, net, time, rng)  
- **Modules** — расширения и диагностика (`doctor.sh`, `autosave.sh`)  
- **Bin/VX** — универсальный интерфейс команд (`vx status | log | version | doctor`)  
- **Style** — оформление в эстетике Neon Aurora  
- **Docs** — документация и схемы  

---

### ⚙️ Команды VX
```bash
vx version     # версия системы
vx status      # текущий статус ядра и git
vx doctor      # диагностика модулей и диска
vx log 10      # последние коммиты
vx boot        # ручной запуск boot/teleport.sh
