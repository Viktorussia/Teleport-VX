#include "space.h"

// Глобальный список пространств
space_t *space_list_head = 0;

// Создание нового Space
space_t* space_create(space_type_t type) {
    // Пока заглушка
    return 0;
}

// Уничтожение Space
void space_destroy(int space_id) {
    // Заглушка
}

// Получение Space по ID
space_t* space_get(int space_id) {
    // Заглушка
    return 0;
}